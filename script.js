// Add a dynamic animation when buttons are clicked
document.querySelectorAll('.explore-btn').forEach(button => {
  button.addEventListener('click', () => {
    alert("Stay tuned! This section is under construction.");
  });
});
